apiVersion: v1
kind: Pod 
metadata:
  name: postgres-pod
  labels:
    app: voting-app 
    name: postgres-pod 

spec:
  containers:
    - name: postgres-pod
      image: postgres
      env:
        - name: POSTGRES_USER
          value: "postgres"
        - name: POSTGRES_PASSWORD
          value: "postgres"