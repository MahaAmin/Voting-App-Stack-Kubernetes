apiVersion: v1
kind: Service 
metadata:
  name: postgres
  labels:
    app: voting-app
    name: postgres

spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: voting-app 
    name: postgres-pod